# FastAPI and server
fastapi==0.104.1
uvicorn[standard]==0.24.0
python-multipart==0.0.6

# MongoDB (async driver)
motor==3.3.2
pymongo==4.6.1

# Validation
pydantic==2.5.0
pydantic-settings==2.1.0
email-validator==2.1.0

# OpenAI (pin httpx 0.27.x: openai 1.3.x passes proxies= to httpx.Client; httpx 0.28+ removed it)
httpx==0.27.2
openai==1.3.7

# Email: SMTP via stdlib (smtplib) - no extra package needed

# Background jobs
apscheduler==3.10.4

# Security (JWT)
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
python-dotenv==1.0.0

# Payments (Razorpay demo)
razorpay==1.4.1

# Utils
python-dateutil==2.8.2